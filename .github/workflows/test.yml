name: Test

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: 'node:16.15.0-buster'

    steps:
      - name: Checkouut
        uses: actions/checkout@v2

      - name: Install
        run: npm ci

      - name: Test
        run: npm t;
